---
import { Image } from 'astro:assets';
import type { Locale } from '../i18n/ui';
import { useTranslations } from '../i18n/utils';
import type { ResolvedProject } from '../lib/content';

interface Props {
  lang: Locale;
  project: ResolvedProject;
  eager?: boolean;
  headingLevel?: 2 | 3;
}

const { lang, project, eager = false, headingLevel = 2 } = Astro.props;
const t = useTranslations(lang);
const { data } = project.entry;
const HeadingTag = headingLevel === 2 ? 'h2' : 'h3';
---

<article class="group relative flex h-full flex-col overflow-hidden border border-edge bg-surface-raised transition-colors hover:border-accent">
  <div class="overflow-hidden">
    <Image
      src={data.cover}
      alt={data.coverAlt}
      widths={[400, 800]}
      sizes="(min-width: 1024px) 22rem, (min-width: 640px) 45vw, 92vw"
      class="aspect-video w-full object-cover"
      loading={eager ? 'eager' : 'lazy'}
    />
  </div>

  <div class="flex flex-1 flex-col gap-3 p-4">
    <HeadingTag class="text-lg leading-snug">
      <a href={project.href} class="after:absolute after:inset-0 group-hover:text-accent">
        {data.title}
      </a>
    </HeadingTag>

    <p class="flex-1 text-sm text-fg-muted">{data.description}</p>

    <ul class="flex flex-wrap gap-1.5" aria-label={t('projects.stack')}>
      {
        data.stack.slice(0, 4).map((item) => (
          <li class="border border-edge bg-surface-raised px-1.5 py-0.5 font-mono text-[0.7rem] text-fg-muted">
            {item}
          </li>
        ))
      }
      {
        data.stack.length > 4 ? (
          <li class="font-mono text-[0.7rem] text-fg-muted">+{data.stack.length - 4}</li>
        ) : null
      }
    </ul>

    <div class="relative z-10 flex flex-wrap items-center gap-3 border-t border-edge pt-3 font-mono text-xs">
      <span class="text-accent" aria-hidden="true">
        {t('projects.readMore')}
        <span> -&gt;</span>
      </span>
      {
        data.repoUrl ? (
          <a
            href={data.repoUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="text-fg-muted hover:text-accent"
          >
            {t('projects.repo')}
          </a>
        ) : null
      }
      {
        data.demoUrl ? (
          <a
            href={data.demoUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="text-fg-muted hover:text-accent"
          >
            {t('projects.demo')}
          </a>
        ) : null
      }
    </div>
  </div>
</article>
